<UserControl x:Class="TwitchClipper.Desktop.Views.ClipMontageFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="SCR-003 New Clip Montage Job" FontSize="24" FontWeight="SemiBold" />

        <TextBlock Grid.Row="1" Text="Streamer names" Margin="0,8,8,8" />
        <TextBox Grid.Row="1" Grid.Column="1" Height="80" AcceptsReturn="True" Text="{Binding StreamerNamesText, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="SCR003-CMP001" />

        <TextBlock Grid.Row="2" Text="Current videos directory" Margin="0,8,8,8" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentVideosDir, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="SCR003-CMP002" />

        <TextBlock Grid.Row="3" Text="Apply overlay" Margin="0,8,8,8" />
        <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding ApplyOverlay}" AutomationProperties.Name="SCR003-CMP003" />

        <TextBlock Grid.Row="4" Text="Max clips" Margin="0,8,8,8" />
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding MaxClips, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="SCR003-CMP004" />

        <TextBlock Grid.Row="5" Text="Scrape pool size" Margin="0,8,8,8" />
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding ScrapePoolSize, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="SCR003-CMP005" />

        <TextBlock Grid.Row="6" Text="Per streamer K" Margin="0,8,8,8" />
        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding PerStreamerK, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="SCR003-CMP006" />

        <TextBlock Grid.Row="7" Grid.ColumnSpan="2" Foreground="DarkRed" Text="{Binding FormAlert}" TextWrapping="Wrap" />

        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Submit Clip Montage Job" Command="{Binding SubmitCommand}" Margin="0,0,8,0" AutomationProperties.Name="SCR003-CMP007" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" AutomationProperties.Name="SCR003-CMP008" />
        </StackPanel>
    </Grid>
</UserControl>
